syntax = "proto3";

package proto;

message Request {
    string token = 1;
}

message Response {
    string name = 1;
    string username = 2;
    string email = 3;
}

service GetService {
    rpc Get(Request) returns (Response);
}