name: Deploy to server nugrohosamiyono.com

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
      with: 
        clean: false
    - name: Build Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15.x
    - name: copy file via ssh password
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
        source: ./
        target: ${{ secrets.PATH_FOLDER_REMOTE_HTTP }}
    - name: copy file via ssh password
      uses: appleboy/scp-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
        source: ./
        target: ${{ secrets.PATH_FOLDER_REMOTE_GRPC }}
    - name: Restart app http
      uses: garygrossgarten/github-action-ssh@release
      with:
        command: |
          cd ${{ secrets.PATH_FOLDER_REMOTE_HTTP }}
          cp ${{ secrets.PATH_FOLDER_REMOTE_HTTP }}/deploy/domainesia/restart-app.sh.domainesia ./restart-app.sh
          bash ./restart-app.sh
        tryKeyboard: true
        host: ${{ secrets.HOST }}
        port: ${{ secrets.PORT }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        target: ${{ secrets.PATH_FOLDER_REMOTE_HTTP }}
    - name: Restart app grpc
      uses: garygrossgarten/github-action-ssh@release
      with:
        command: |
          cd ${{ secrets.PATH_FOLDER_REMOTE_GRPC }}
          cp ${{ secrets.PATH_FOLDER_REMOTE_GRPC }}/deploy/domainesia/restart-app.sh.domainesia ./restart-app.sh
          bash ./restart-app.sh
        tryKeyboard: true
        host: ${{ secrets.HOST }}
        port: ${{ secrets.PORT }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        target: ${{ secrets.PATH_FOLDER_REMOTE_GRPC }}
        
